node_modules
build
.DS_Store
.vscode
.vercel
.turbo
dist
.env
# The local-env family, matching apps/web/.gitignore. The root only listed `.env`, so a
# `.env.local` here — which is where you put credentials when running turbo from the repo root —
# was untracked but not ignored, and `git add .` would stage it.
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.sentry-build-plugin
pgdata
replay.log
.output
.nitro
tmp
.worktrees
apps/web/AGENTS.md
apps/web/CLAUDE.md
