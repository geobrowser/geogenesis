import { TopicRecordPage, type TopicRecordPageProps } from '../topic-record-page';

export default function TopicOverviewPage(props: TopicRecordPageProps) {
  return <TopicRecordPage {...props} />;
}
