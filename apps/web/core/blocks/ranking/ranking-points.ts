/**
 * Competition-linked points (Borda scores, "N points" labels).
 *
 * Enable when a competition entity in the root space references this ranking block.
 * Follow-up ticket: resolve that relation and set `RANKING_POINTS_UI_ENABLED` to true.
 */
export const RANKING_POINTS_UI_ENABLED = false;
