/** Shared "Requested · Under vote" label so editor and member request buttons stay identical. */
export const UnderVote = () => (
  <div className="inline-flex items-center gap-1">
    <span>Requested</span>
    <span>·</span>
    <span className="text-grey-04">Under vote</span>
  </div>
);
