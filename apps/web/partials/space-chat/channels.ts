import type { SpaceChatChannel } from './types';

export const DEFAULT_SPACE_CHAT_CHANNEL: SpaceChatChannel = {
  id: 'general',
  name: 'Chat',
  description: 'Space-wide discussion and coordination.',
};

export const DEFAULT_SPACE_CHAT_CHANNEL_ID = DEFAULT_SPACE_CHAT_CHANNEL.id;
