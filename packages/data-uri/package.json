{
  "name": "@geogenesis/data-uri",
  "version": "0.0.1",
  "private": true,
  "main": "dist",
  "scripts": {
    "build": "tsc lib/index.ts --outDir dist -d",
    "test": "yarn build:wasm && ts-node test/index.ts",
    "build:wasm": "asc test/assembly.ts -o build/test.wasm -t build/test.wat --debug --runtime stub --exportRuntime --bindings raw"
  },
  "dependencies": {
    "@ipld/dag-cbor": "^7.0.1",
    "multiformats": "^9.6.4"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "^0.27.0",
    "@types/node": "^17.0.31",
    "assemblyscript": "^0.20.6",
    "expect": "^28.0.2",
    "ts-node": "^10.7.0",
    "typescript": "4.6.4"
  }
}
