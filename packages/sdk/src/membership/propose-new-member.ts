import { simulateContract } from 'viem/actions';
import { ProposalMetadata } from '../..';

export function proposeNewMember(metadata: ProposalMetadata) {
  // const config = simulateContract({
  //   abi
  // })
}