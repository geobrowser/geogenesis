```sh
# Build library
pnpm build

# Build web app in watch mode. This is useful when you're actively developing the library
# and running it in the web app since it will automatically refresh the web app with your
# new changes.
pnpm build --watch

# Run tests
pnpm test

# Run tests in watch mode
pnpm test --watch
```
